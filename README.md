# Shared base Renovate configuration

* limits updates to NPM, Go, Docker
* updates external dependencies only once per week
* separates internal and external updates
* separates major and non-major version bumps
